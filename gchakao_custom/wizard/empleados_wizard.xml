<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="empleados_wizard" model="ir.ui.view">
            <field name="name">Empleados</field>
            <field name="model">empleados.wizard</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <field name="company_id" invisible="1"/>
                        <field name="assigned_employees" invisible="1"/>
                        <field name="employee_ids" nolabel="1" widget="many2many">
                            <tree>
                                <field name="name" force_save="1"/>
                                <field name="contract_id"/>
                            </tree>
                        </field>
                    </sheet>
                    <footer>
                        <button name="action_transferir" string="Transferir" type="object" class="btn-primary"/>
                        <button string="Cancelar" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="empleados_wizard_action" model="ir.actions.act_window">
            <field name="name">Empleados</field>
            <field name="res_model">empleados.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>